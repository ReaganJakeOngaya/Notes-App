services:
  - type: web
    name: noteflow-backend
    runtime: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
      flask db upgrade
    startCommand: python -m gunicorn --worker-class gevent --workers 2 backend.app.run:create_app
    env: python
    pythonVersion: "3.9.13"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: FLASK_APP
        value: backend.app
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: noteflow-db
          property: connectionString
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: OAUTH_REDIRECT_URI
        value: https://noteflow-backend.onrender.com/auth/google/callback
      - key: PYTHONPATH
        value: /opt/render/project/src
      - key: UPLOAD_FOLDER
        value: /opt/render/project/src/backend/static/uploads

databases:
  - name: noteflow-db
    plan: free
    databaseName: noteflow
    user: noteflow